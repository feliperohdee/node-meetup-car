{
  "name": "remote",
  "version": "1.0.0",
  "description": "",
  "main": "__build__/server.js",
  "scripts": {
    "start": "node __build__/server.js",
    "commit": "npm run compile && git add . && git commit -m 'do changes' && git push origin remote-server",
    "compile": "rm -rf __build__ && tsc",
    "deploy": "git push heroku remote-server:master && heroku logs --source app --tail",
    "serve": "npm run compile && node_modules/.bin/http-server . -p 9090"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rxjs": "^5.0.0-beta.7",
    "ws": "^1.1.0"
  },
  "devDependencies": {
    "http-server": "^0.9.0"
  }
}
